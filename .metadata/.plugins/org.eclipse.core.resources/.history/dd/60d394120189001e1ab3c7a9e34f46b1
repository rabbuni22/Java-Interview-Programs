import java.util.Arrays;

public class Even {
	public static void main(String[] args) {
		int arr[]= {10,2,5,1,66,3};
		Arrays.parallelSort(arr);
		Arrays.stream(arr).forEach(n -> System.out.print(n + " "));

	
	}
}
